#include <stdio.h>
main()
{
    int formin, formout, forminX = 1;
    int digits,tenthdg;
    int digit[digits];
    int temp, i;

    scanf("%d",&formin);
    scanf("%d",&digits);
    for (i=0;i<digits;i++){
        scanf("%d",&digit[i]);
    }
    scanf("%d",&formout);

    temp = formin;
    for (i=0;i<digits;i++)(
        temp = formin;
        temp *=
    )

    for (i=0;i<digits;i++)(
    tenthdg +=  digit[i]*formin[j]
    )







    if (formin == formout){
        for (i=0;i<digits;i++){
            printf("%d\n",digit[i]);
        }
    }
    else{
        switch (formin){
            case 2:
                switch (formout){
                    case 8:

                    case 10:

                break;
                }
            case 8:
                switch (formout){
                    case 2:

                    case 10:

                break;
                }
            case 10:
                switch (formout){
                    case 2:

                    case 8:

                break;
                }
        break;
        }
    }
}
