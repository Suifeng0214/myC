#include <stdio.h>
main()
{
    int formin, formout;
    int digit[digits], digits;
    int temp;

    scanf("%d",&formin);
    scanf("%d",&digits);
    for (i=0;i<digits;i++){
        scanf("%d",digit[i])
        }
    scanf("%d",&formout);


    if (formin == formout){
        for (i=0;i<digits;i++){
            printf("%d\n",digit[i])
        }
    else{
       switch (formin){

       }
    }
    }
}
